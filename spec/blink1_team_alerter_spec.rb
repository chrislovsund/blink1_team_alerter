require 'spec_helper'

describe Blink1TeamAlerter do
  it 'has a version number' do
    expect(Blink1TeamAlerter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
